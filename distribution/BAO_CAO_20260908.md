# Kiểm tra cảnh báo virus và đóng gói — 08/09/2026

## Kết luận có bằng chứng

- Defender event 1116 ngày 03/09/2026 08:50:19 ghi nhận `Trojan:Win32/MalUri.A!cl`, ThreatID `2147843494`, đối với `C:\Users\ADMIN\OneDrive\Desktop\TTSK_Dim_Plates[Fix_Section-1-9-26].zip` tải từ `https://onedrive.live.com/`. Detection Origin: Internet; Type: FastPath; Source: Downloads and attachments. Không có tên tệp con trong ZIP. Nhật ký hiện báo IsActive=False, DidThreatExecute=False.
- Đây là cảnh báo malware cụ thể, không chỉ là thông báo thiếu reputation/chữ ký. Chưa có đủ bằng chứng để xác nhận ZIP cũ là false positive hoặc xác định dòng mã gây nhận diện. Bản checkout hiện tại không đồng nhất với ZIP ngày 01/09.
- Đã kiểm tra thư mục Google Drive người dùng gửi qua connector: `https://drive.google.com/drive/folders/12VAjb74D0ADe4kwJf6C1j3c5aqKk3On6`. File `TTSK_Dim_Plates[8-9-26].zip`, ID `1gkG1NtqOM4cwAZ3oZzT51y3q3y-WdHga`, dung lượng **294.185.263 byte**, `can_download=true`. File vượt giới hạn quét 100 MB Google công bố. Điều này giải thích được cảnh báo không thể quét do kích thước, nếu đúng nội dung cảnh báo người dùng thấy; metadata không chứng minh hoặc phủ nhận malware flag. Chưa nhận nguyên văn cảnh báo và chưa tải/phân tích ZIP cloud này.
- EXE portable và EXE Release hiện chưa ký Authenticode. Không thấy chứng thư code signing trong CurrentUser/My hoặc LocalMachine/My. Chữ ký thiếu là hạn chế phát hành, không phải bằng chứng nguyên nhân của MalUri.
- Kiểm tra các điểm Process.Start, Base64, assembly loading và Win32 interop: worker phục vụ Hole Mark/PDF, Base64 truyền định danh drawing/đường dẫn PDF, DLL tải từ Tekla cài trên máy. Không thay đổi các luồng này. Tìm kiếm tĩnh có giới hạn, không thay thế phân tích malware đầy đủ.
- Tổng dung lượng tệp checkout trước thay đổi: 236.634.232 byte, gồm lịch sử/cache/build. Đây KHÔNG phải dung lượng ZIP cũ; không thể dùng số này kết luận ZIP cũ vượt giới hạn Google.

## Các thay đổi đã thực hiện

1. `distribution/Build-CloudPackage.ps1`: đóng gói từ build theo dependency và Content của csproj; kiểm tra tệp thiếu, hash sao chép, loại tệp dev/cấu hình cá nhân, quét thư mục và ZIP bằng Defender; xuất manifest, hash ZIP, validation JSON. Không upload hoặc thay đổi thiết lập antivirus. Mặc định từ chối EXE chưa ký; `-AllowUnsigned` dành cho bản chẩn đoán.
2. `distribution/README.md`: hướng dẫn build, đóng gói, ký và báo nhận nhầm.
3. `README.md`: thêm liên kết quy trình chia sẻ qua cloud.
4. `Tao_Phim_Tat_Desktop.bat`: bỏ ExecutionPolicy Bypass không cần thiết; báo lỗi và exit 1 nếu thiếu EXE hoặc thao tác PowerShell thất bại. Đây là vệ sinh script, chưa chứng minh liên quan tới cảnh báo cũ.
5. Báo cáo này.

Không sửa C#, csproj, worker, DIM, geometry, layout hoặc PDF. Không chạy script `Cap_Nhat_Portable.bat` vì nó có git add/commit/push tự động. Không ghi đè portable hiện dùng hoặc các thay đổi MainForm có sẵn.

## Sao lưu và kiểm tra

- Backup: `TTSK Dim Plates/TTSK Dim Plates/backup/001_20260908-cloud-antivirus/`; chứa README/BAT gốc đã đối chiếu hash, trạng thái Git ban đầu và `source-before.csv`.
- Đối chiếu 76 tệp nguồn/tài nguyên ngoài bin/obj/backup: **0 tệp thay đổi**.
- `dotnet msbuild ... /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m /nologo /v:minimal`: exit 0. Vẫn có cảnh báo biến/field không dùng, field chưa gán và unreachable code trong các tệp không chỉnh sửa; không xóa nhánh thuật toán để xử lý cảnh báo.
- Quét toàn checkout bằng MpCmdRun custom scan: exit 0, found no threats. Quét không tự khắc phục (`-DisableRemediation`); bảo vệ thời gian thực vẫn bật.
- Gói runtime: 37 tệp từ build + README + manifest. Giải nén và kiểm tra **38/38 SHA-256 khớp**, không có tệp thừa ngoài manifest, không có BAT/CMD/PS1/LNK/PDB/LOG/CFG.
- Defender thư mục runtime và ZIP: cả hai exit 0, found no threats, security intelligence `1.459.105.0`.
- Parser PowerShell của script đóng gói và lệnh tạo shortcut: đạt. Nhánh mặc định từ chối unsigned: đạt. Không thực thi tạo shortcut, không mở Tekla, không chạy DIM/PDF trên model.
- `git diff --check` cho README/BAT: không lỗi whitespace; có thông báo chuẩn hóa LF/CRLF.

## Sản phẩm

ZIP: `.codex-artifacts/cloud-release/TTSK-Dim-Plates-UNSIGNED-DIAGNOSTIC-20260908-163253-02836a1e/TTSK-Dim-Plates-UNSIGNED-DIAGNOSTIC-20260908-163253-02836a1e.zip`

Dung lượng: **4.983.700 byte**.

SHA-256 ZIP: `0F4CFFD3E69E0F1C6EAB6CBA621B11424F3D8DF91CD6C51839E01597A73C0EAA`.

SHA-256 EXE rebuild: `441F31AC1017F06BE4C3823831585CBECB81AE5EBC57275E90027E9EB6F2A243`.

SHA-256 EXE portable giữ nguyên: `7077A136F2D6CEF90BF384571511B28FB964153F6B9100E2D8D5FAAFAA960365`.

Hai EXE có hash khác nhau; gói mới dùng bản rebuild từ nguồn hiện tại. Không suy diễn hash khác là nhiễm virus.

## Phần chưa thể xác nhận

Đã kiểm tra metadata file Google Drive; chưa xác minh giao diện cảnh báo hoặc quét nội dung ZIP cloud. Chưa có liên kết file OneDrive, chưa gửi mẫu/upload gói mới lên dịch vụ, chưa ký bằng chứng thư tin cậy, chưa thử live Tekla/PDF. Local scan sạch không chứng minh cloud đã bỏ cờ. Cần nội dung cảnh báo của đúng file; nếu xác nhận false positive, gửi yêu cầu đánh giá tới nhà cung cấp với tên detection và SHA-256, không tắt antivirus hoặc che giấu file để vượt chặn.

Nguồn chính thức đã đối chiếu:

- https://learn.microsoft.com/en-us/defender-xdr/developer-faq
- https://learn.microsoft.com/en-us/troubleshoot/sharepoint/security/false-positive-malware-detections
- https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation
- https://support.google.com/drive/answer/141702
